# <data-dir>/<name>.jsonl files are used for supervised fine-tuning

# tuning datasets
  # max_train_samples: max number of samples to use for training. -1 means all. 0 means not to use.
  # split_dev: whether to split the dataset into training and validation datasets. If false, the dataset is used for training only.
  # upsampling_factor: upsampling factor for the dataset. 1 means no upsampling. Valid for both training and validation datasets.
datasets:
  ac_002:
    max_train_samples: -1 # -1 means all
    split_dev: false
    upsampling_factor: 16
  calm3_22b_chat_20241018083433--Qwen2.5_32B_Instruct_20241022115410:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  calm3_22b_chat_20241022133932--Qwen2.5_32B_Instruct_20241024100350:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  calm3_22b_chat_20241022155627--Qwen2.5_32B_Instruct_20241024144245:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  daring_anteater_en:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  format:
    max_train_samples: -1
    split_dev: false
    upsampling_factor: 1
  ichikara_004_002_simpleanswer:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  ichikara_004_002_single:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  logical_math_coding_wizard8x22b:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  multiturn_calm3_orig:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  random_to_fixed_multiturn_calm3_orig:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  jaster_v1.4.1:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  llmjp_extraction_v0.1:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
  llmjp_extraction_v0.2:
    max_train_samples: -1
    split_dev: true
    upsampling_factor: 1
# number of dev samples are the minimum value of {max_dev_samples, max_dev_ratio * number of dev samples} in the dataset.
max_dev_samples: 100
max_dev_ratio: 0.1
